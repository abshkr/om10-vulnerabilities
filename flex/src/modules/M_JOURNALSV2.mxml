<?xml version="1.0" encoding="utf-8"?>
<s:Module xmlns:fx="http://ns.adobe.com/mxml/2009" 
		  xmlns:s="library://ns.adobe.com/flex/spark" 
		  xmlns:mx="library://ns.adobe.com/flex/mx" xmlns:views="views.*"
		  implements="IModuleInterface" width="100%" height="100%">
	<fx:Script>
		<![CDATA[
			import mx.events.FlexEvent;
			
			private var callback:Function;
			
			private var _state:String;
			public var frme:Object=null;
			
			public function setCallback(func:Function): void
			{
				callback = func;
				trace(">>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>");
			}
			
			public function setState(value:String): void
			{
				trace(value);
				view.currentState = value;
			}
			
			public function setSize(w:int, h:int): void
			{
				this.height=h;
				this.width=w;
			}
			
			public function setSecurity(isRead:Boolean=true, isUpdate:Boolean=false, isCreate:Boolean=false, isDelete:Boolean=false, isPassword:Boolean=false): void
			{
				view.readOnly = isRead;
				view.canUpdate = isUpdate;
				view.canCreate = isCreate;
				view.canDelete = isDelete;
				view.hasPassword = isPassword;
			}
			
			public function setFramePosition(): void
			{
				//frme.invalidateDisplayList();
			}
			
			protected function view_creationCompleteHandler(event:FlexEvent):void
			{
				//width = view.width;
				//height = view.height;
			}
		]]>
	</fx:Script>
	<fx:Declarations>
		<!-- Place non-visual elements (e.g., services, value objects) here -->
	</fx:Declarations>
	<views:v_Testbed id="view" creationComplete="view_creationCompleteHandler(event)"/>
</s:Module>
