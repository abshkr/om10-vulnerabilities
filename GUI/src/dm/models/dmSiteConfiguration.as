package dm.models
{
	public dynamic class dmSiteConfiguration extends dmModel
	{	
		public function dmSiteConfiguration( params : * = false )
		{
			super(params);
			this.dmClass = 'dmSiteConfiguration';
		}
	}		
}