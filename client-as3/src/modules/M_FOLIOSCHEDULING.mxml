<?xml version="1.0" encoding="utf-8"?>
<s:Module xmlns:fx="http://ns.adobe.com/mxml/2009" 
		  xmlns:s="library://ns.adobe.com/flex/spark" width="100%" height="100%"
		  xmlns:mx="library://ns.adobe.com/flex/mx" xmlns:views="views.*" implements="IModuleInterface">
	<fx:Script>
		<![CDATA[
			import mx.events.FlexEvent;
			
			private var callback:Function;
			
			private var _state:String;
			public var frme:Object=null;
			
			public function setCallback(func:Function): void
			{
				callback = func;
				trace(">>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>");
			}
			
			public function setState(value:String): void
			{
				trace(value);
				view.currentState = value;
			}
			
			public function setSize(w:int, h:int): void
			{
				this.height=h;
				this.width=w;
			}
			
			public function setSecurity(isRead:Boolean=true, isUpdate:Boolean=false, isCreate:Boolean=false, isDelete:Boolean=false, isPassword:Boolean=false): void
			{
				view.controller.readOnly = isRead;
				view.controller.canUpdate = isUpdate;
				view.controller.canCreate = isCreate;
				view.controller.canDelete = isDelete;
				view.controller.hasPassword = isPassword;
				trace("set Security of folioScheduling", isUpdate, view.controller.canUpdate)
			}
			
			public function setFramePosition(): void
			{
				//frme.invalidateDisplayList();
			}
			
			protected function view_creationCompleteHandler(event:FlexEvent):void
			{
				if (callback != null) callback();
			}
		]]>
	</fx:Script>
	<fx:Declarations>
		<!-- Place non-visual elements (e.g., services, value objects) here -->
	</fx:Declarations>
	<views:v_FolioScheduling id="view" creationComplete="view_creationCompleteHandler(event)"/>
	
</s:Module>
