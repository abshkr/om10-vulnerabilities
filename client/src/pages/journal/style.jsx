import styled from 'styled-components';

const JournalContainer = styled.div`
  & .ant-tabs-nav-wrap {
    display: none;
  }

  .ant-tabs-bar {
    display: none;
  }
`;

export { JournalContainer };
