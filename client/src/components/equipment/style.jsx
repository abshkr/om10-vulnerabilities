import styled from 'styled-components';

const EquipmentImage = styled.div`
  display: flex;
  justify-content: center;
  transform: scale(0.6);
  margin-top: -20px;
  margin-bottom: -40px;
`;

export { EquipmentImage };
